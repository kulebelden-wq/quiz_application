
--------------------------------------------------
End of Report
--------------------------------------------------



Great, let’s go through this code carefully, line by line, and I’ll explain **what each part does, why it’s used, and how it fits into the app’s logic**. This is an Android app written in Java, specifically a **QuizActivity** that runs a quiz game.

---

### **Package and Imports**
```java
package com.example.quizapp;
```
- Defines the namespace of the app. Keeps code organized and avoids conflicts with other libraries.

```java
import android.content.Intent;
import android.content.SharedPreferences;
import android.content.res.ColorStateList;
import android.icu.util.ULocale;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.ImageView;
import android.widget.LinearLayout;
import android.widget.ProgressBar;
import android.widget.TextView;
import android.widget.Toast;
import androidx.appcompat.app.AppCompatActivity;
import java.util.ArrayList;
import java.util.List;
import java.util.Locale;
import org.json.JSONArray;
import org.json.JSONObject;
```
- These are **imports**: they bring in Android classes (UI widgets, layouts, system utilities) and Java classes (lists, JSON handling).
- Example: `Intent` is used to move between activities, `Toast` shows short messages, `ProgressBar` shows quiz progress.

---

### **Class Declaration**
```java
public class QuizActivity extends AppCompatActivity {
```
- Defines the main quiz screen.
- Extends `AppCompatActivity`, meaning it’s an Android screen with lifecycle methods (`onCreate`, etc.).

---

### **UI Components**
```java
private TextView tvQuestion, tvQuestionNo, tvCategoryTitle, tvScenario, tvExplanationText;
private ProgressBar progressBar;
private Button btnOp1, btnOp2, btnOp3, btnOp4, btnNext;
private LinearLayout layoutExplanation, btnToggleExplanation;
private ImageView ivExplanationArrow;
```
- These are **references to UI elements** in the XML layout (`activity_quiz.xml`).
- Example: `tvQuestion` shows the question text, `btnOp1`–`btnOp4` are the answer buttons, `btnNext` moves to the next question.

---

### **Quiz State Variables**
```java
private List<Question> allQuestions;
private List<Question> filteredQuestions;
private int currentQuestionIndex = 0;
private int score = 0;
private int selectedOptionIndex = -1;
private boolean isAnswered = false;
private boolean isExplanationExpanded = false;
private String selectedCategory;
```
- These variables track quiz progress:
  - `allQuestions`: all available questions.
  - `filteredQuestions`: only questions for the chosen category.
  - `currentQuestionIndex`: which question is being shown.
  - `score`: number of correct answers.
  - `selectedOptionIndex`: which option the user picked.
  - `isAnswered`: prevents multiple answers for the same question.
  - `isExplanationExpanded`: toggles showing/hiding explanations.
  - `selectedCategory`: stores the chosen quiz category (e.g., Math, Physics).

---

### **onCreate Method**
```java
@Override
protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    setContentView(R.layout.activity_quiz);
```
- `onCreate` runs when the activity starts.
- `setContentView` loads the layout file.

```java
selectedCategory = getIntent().getStringExtra("CATEGORY");
```
- Retrieves the category passed from the previous screen (e.g., "Math").

```java
tvCategoryTitle.setText(selectedCategory + " Quiz");
```
- Updates the title to show which quiz category is active.

---

### **Loading and Filtering Questions**
```java
loadQuestions();
filterQuestionsByCategory();
```
- `loadQuestions()` fills `allQuestions` with predefined questions.
- `filterQuestionsByCategory()` selects only those matching the chosen category.

```java
if (filteredQuestions.isEmpty()) {
    Toast.makeText(this, "No questions found for this category!", Toast.LENGTH_LONG).show();
    finish();
    return;
}
```
- If no questions exist for the category, show a message and exit.

---

### **Displaying Questions**
```java
displayQuestion();
```
- Shows the current question and options on screen.

---

### **Explanation Toggle**
```java
btnToggleExplanation.setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        isExplanationExpanded = !isExplanationExpanded;
        tvExplanationText.setVisibility(isExplanationExpanded ? View.VISIBLE : View.GONE);
        ivExplanationArrow.setRotation(isExplanationExpanded ? 180 : 0);
    }
});
```
- Lets the user expand/collapse the explanation for each question.
- Rotates the arrow icon when expanded.

---

### **Option Buttons**
```java
View.OnClickListener optionClickListener = new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        if (isAnswered) return;

        resetOptionButtons();
        v.setBackgroundTintList(ColorStateList.valueOf(getResources().getColor(R.color.orange_primary)));

        if (v.getId() == R.id.btnOption1) selectedOptionIndex = 0;
        else if (v.getId() == R.id.btnOption2) selectedOptionIndex = 1;
        else if (v.getId() == R.id.btnOption3) selectedOptionIndex = 2;
        else if (v.getId() == R.id.btnOption4) selectedOptionIndex = 3;
    }
};
```
- Handles option selection:
  - Prevents answering twice (`isAnswered` check).
  - Highlights the selected button.
  - Stores which option was chosen.

---

### **Next Button**
```java
btnNext.setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        if (selectedOptionIndex == -1) {
            Toast.makeText(QuizActivity.this, "Please select an option", Toast.LENGTH_SHORT).show();
            return;
        }

        if (!isAnswered) {
            checkAnswer();
        } else {
            currentQuestionIndex++;
            if (currentQuestionIndex < filteredQuestions.size()) {
                displayQuestion();
            } else {
                Intent intent = new Intent(QuizActivity.this, ResultActivity.class);
                intent.putExtra("SCORE", score);
                intent.putExtra("TOTAL", filteredQuestions.size());
                intent.putExtra("CATEGORY", selectedCategory);
                startActivity(intent);
                finish();
            }
        }
    }
});
```
- If no option is selected, shows a warning.
- If the question hasn’t been answered yet → calls `checkAnswer()`.
- If already answered → moves to the next question.
- If quiz is finished → opens `ResultActivity` with score and category.

---

### **loadQuestions Method**
This is where all the **quiz content** is defined:
```java
if ("Technology".equals(selectedCategory)) {
    allQuestions.add(new Question("Technology", "A developer wants to make their website look different on mobile and desktop using CSS.", "What is the best technique to use?", "Flexbox", "Media Queries", "JavaScript", "HTML5", 1, "Media Queries allow you to apply different styles based on the device's screen size."));
    ...
}
```
- Adds questions depending on category (Technology, Math, Physics, Riddles, Logic).
- Each `Question` object stores:
  - Category
  - Scenario
  - Question text
  - Four options
  - Correct answer index
  - Explanation

---

✅ **In summary:**
- This code builds a **quiz app** with multiple categories.
- It loads questions, filters them by category, displays them one by one, checks answers, shows explanations, and finally displays results.
- Each part of the code is essential for handling **UI interaction, quiz logic, and navigation**.

---

Would you like me to also **walk through the `checkAnswer()` and `displayQuestion()` methods** (since they’re crucial for scoring and showing questions), or should I focus only on what you uploaded so far?


------thses are the proceeding of the result.java------------
================================================================================
int score = getIntent().getIntExtra("SCORE", 0);
        int total = getIntent().getIntExtra("TOTAL", 10);
        String category = getIntent().getStringExtra("CATEGORY");

        tvCategory.setText(category + " - Beginner");

        int percentage = (score * 100) / total;
        tvScore.setText(percentage + "%");
        tvScoreDetail.setText(score + "/" + total + " correct");

        btnMainMenu.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intent = new Intent(ResultActivity.this, LandingActivity.class);
                intent.setFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP);
                startActivity(intent);
                finish();
            }
        });

        btnRestart.setText("Restart Quiz");
        btnRestart.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intent = new Intent(ResultActivity.this, QuizActivity.class);
                intent.putExtra("CATEGORY", category);
                startActivity(intent);
                finish();
            }
        });